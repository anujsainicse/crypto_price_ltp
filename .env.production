# Production Environment Configuration
# =====================================
# IMPORTANT: Keep this file secure! Never commit to version control.
# Set file permissions: chmod 600 .env.production
#
# This file should be copied to your VPS and loaded when deploying:
# docker-compose --env-file .env.production up -d

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379

# CRITICAL: Set a strong Redis password before deployment!
# Generate with: openssl rand -base64 32
# Replace the placeholder below with your generated password
REDIS_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD

REDIS_DB=0
REDIS_TTL=7200  # 2 hours data retention

# Application Configuration
LOG_LEVEL=INFO
LOG_DIR=logs

# Service Configuration
SERVICE_RESTART_DELAY=10  # Wait 10 seconds before restart attempts
SERVICE_MAX_RETRIES=5     # Maximum restart attempts

# Monitoring
HEALTH_CHECK_INTERVAL=60   # Check health every 60 seconds
STATUS_UPDATE_INTERVAL=60  # Update status every 60 seconds

# =====================================
# Bot Integration Instructions:
# =====================================
# Your bot application should connect to Redis using:
#   Host: localhost (or 127.0.0.1)
#   Port: 6379
#   Password: <same as REDIS_PASSWORD above>
#   DB: 0
#
# Example connection string:
#   redis://:REDIS_PASSWORD@localhost:6379/0
#
# Available data keys:
#   - bybit_spot:BTCUSDT
#   - coindcx_futures:B-BTC_USDT
#   - delta_futures:BTCUSD
#   - delta_options:C-BTC-108200-211025
